G-ACE Universal Webhook Gateway v1.2.2 Server Core five-stage + TGServer log validation result

Local validation completed in /mnt/data/webhook-gateway-v123-5stage after rebuilding the source layout into the Server Core five-stage architecture and adding TGServer log aggregation.

Architecture target:
- Part -> Feature -> Component -> System -> Application

Commands executed after the rebuild:
1. npm ci --no-audit --no-fund
   - success in local ZIP workspace with package-lock.json
2. npm run build
   - success
3. npm test
   - success, 13 tests passed
4. npm audit --audit-level=high
   - success, found 0 vulnerabilities

Repository connector note:
- GitHub connector tree update does not include package-lock.json in this commit due payload size limits.
- CI uses npm install --no-audit --no-fund in this commit.
- The validated ZIP artifact retains package-lock.json; add it from a server/PC clone pass when deterministic npm ci is required on main.

Structure verification:
- src/part exists and contains single-responsibility primitives.
- src/feature exists and contains config, verifier, DB ledger, queue, spool, and TGServer log features.
- src/component exists and contains routing and delivery composition logic.
- src/system exists and contains API and Worker runtime systems.
- src/application exists and contains application launch modules.
- src/server.ts and src/worker.ts are thin launchers only.

Docker validation:
- docker is not installed in this execution environment, so docker compose config / docker compose E2E could not be executed here.
- Docker Compose E2E remains mandatory on the target server after GitHub push.

Preserved v1.2.2 P0 coverage:
- Downstream successMode status_only/status_and_header
- unknown delivery status with maxAttempts -> dead
- DELIVERY_TIMEOUT_MS and per-destination timeout
- replay cooldown and replay audit
- ingress/admin/replay rate limit
- webhook_rate_limited_total metric
- Clock skew readiness check with cache
- webhook_clock_skew_seconds metric
- Standard/Stripe multi-signature support
- Generic HMAC multi-candidate support
- provider_event_id normalization
- STORE_RAW_BODY=false default
- replay from normalized_payload/cloud_event, not body_text
- sanitization for audit/errors/metrics-facing values
- config validation at startup

TGServer log aggregation:
- src/part/logging-types.ts
- src/feature/tgserver-log.ts
- API and Worker systems emit sanitized operational logs
- Sink is non-blocking, batched, bounded, and metric-instrumented
- Provider 202 path is not blocked by TGServer availability
